{
  "name": "Laravel Codespace + QuickAdminPanel",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash",
    "php.validate.executablePath": "/usr/local/bin/php"
  },
  "extensions": [
    "bmewburn.vscode-intelephense-client",
    "onecentlin.laravel-blade",
    "MehediDracula.php-namespace-resolver",
    "shufo.vscode-blade-formatter",
    "ms-azuretools.vscode-docker"
  ],
  "postCreateCommand": "cp .env.example .env && composer update && php artisan key:generate && npm install && npm run build",
  "forwardPorts": [8000, 3306, 8080],
  "portsAttributes": {
    "8000": { "label": "Laravel App", "onAutoForward": "openBrowser" },
    "3306": { "label": "MySQL DB" },
    "8080": { "label": "phpMyAdmin", "onAutoForward": "openBrowser" },
     "6379": { "label": "Redis", "onAutoForward": "ignore" }
  },
  "remoteUser": "vscode"
}
